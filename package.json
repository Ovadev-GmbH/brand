{
  "name": "ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The three Ovadev component libraries, one per brand.",
  "packageManager": "bun@1.3.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "@types/node": "^26.4.1"
  }
}
