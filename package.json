{
  "name": "brand",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Ovadev brands: component libraries, and soon logos, fonts and DESIGN.md.",
  "packageManager": "bun@1.4.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "@types/node": "^26.4.1"
  }
}
