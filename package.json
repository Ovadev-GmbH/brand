{
  "name": "brand",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Ovadev brands: component libraries, and soon logos, fonts and DESIGN.md.",
  "packageManager": "bun@1.4.2",
  "workspaces": [
    "packages/*",
    "site"
  ],
  "scripts": {
    "build": "bun run --filter '@ovadev-gmbh/*' build && bun run --filter site build",
    "check-types": "bun run --filter '*' check-types",
    "dev": "bun run --filter '@ovadev-gmbh/*' build && bun run --filter site dev"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "@types/node": "^26.4.1"
  }
}
