/* The icon set for a React Server Component: Phosphor's SSR build, which
 * reads no context and so renders without "use client". Import from
 * ./icons in client components (it follows IconContext), from ./icons/ssr
 * in server components — in Next's App Router a page or layout. */
export * from "@phosphor-icons/react/ssr";
