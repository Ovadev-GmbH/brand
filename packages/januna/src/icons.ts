/* The icon set, as part of the system: Hugeicons, re-exported so an app
 * imports its icons from the brand package and never names the library.
 * ESM straight through, so an app bundles only the icons it draws. */
export { HugeiconsIcon } from "@hugeicons/react";
export type { HugeiconsProps, IconSvgElement } from "@hugeicons/react";
export * from "@hugeicons/core-free-icons";
