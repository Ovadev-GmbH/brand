# GitHub Packages hosts the @ovadev-gmbh scope. The token comes from the
# environment so nothing secret is ever committed.
@ovadev-gmbh:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
